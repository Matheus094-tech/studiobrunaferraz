import React, { useEffect, useMemo, useState } from "react";
import {
  Container,
  Row,
  Col,
  Button,
  Modal,
  ModalHeader,
  ModalBody,
  ModalFooter,
  Badge,
} from "reactstrap";

export default function GaleriaStub(){
  return (
    <div style={{padding: 32, color: "#fff"}}>
      <h1>/eventos OK</h1>
      <p>Se você está vendo isto, a rota está certa. O problema está no componente real.</p>
    </div>
  );
}
